{
  "name": "konan",
  "version": "1.0.0",
  "private": true,
  "description": "District game with MVP auth backend and progress sync.",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "dev": "node --watch --disable-warning=ExperimentalWarning server.js"
  }
}
